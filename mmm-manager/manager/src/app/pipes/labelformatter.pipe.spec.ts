import { LabelformatterPipe } from './labelformatter.pipe';

describe('LabelformatterPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelformatterPipe();
    expect(pipe).toBeTruthy();
  });
});
