import { Mouse.Enter.Leave.DebounceDirective } from './mouse.enter.leave.debounce.directive';

describe('Mouse.Enter.Leave.DebounceDirective', () => {
  it('should create an instance', () => {
    const directive = new Mouse.Enter.Leave.DebounceDirective();
    expect(directive).toBeTruthy();
  });
});
